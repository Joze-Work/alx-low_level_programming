#include <stdio.h>

/**
  * main - Entry point and prints "with proper grammar, but the outcome is a
  *	   piece of art," using printf function
  *
  * Return: 0 on success.
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
